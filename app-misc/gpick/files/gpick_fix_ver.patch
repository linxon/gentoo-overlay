--- original/gpick.py	2011-05-16 08:44:23.000000000 +0400
+++ gpick-gpick-0.2.4/tools/gpick.py	2017-06-03 10:40:39.565284000 +0300
@@ -109,9 +109,9 @@
 		except OSError, e:
 			revision = ''
 
-		self.Replace(GPICK_BUILD_REVISION = revision,
-			GPICK_BUILD_DATE =  time.strftime ("%Y-%m-%d"),
-			GPICK_BUILD_TIME =  time.strftime ("%H:%M:%S"));	
+		self.Replace(GPICK_BUILD_REVISION = '',
+			GPICK_BUILD_DATE =  time.strftime("%Y-%m-%d"),
+			GPICK_BUILD_TIME =  time.strftime("%H:%M:%S"));
 
 def RegexEscape(str):
 	return str.replace('\\', '\\\\')
